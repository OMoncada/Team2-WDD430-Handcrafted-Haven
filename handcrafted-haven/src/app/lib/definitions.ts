export type Profiles = {
  id: string;
  name: string;
  email: string;
  password: string;
};

export type Products = {
  id: string;
  name: string;
  price: number;
  quantity: number;
};

export type Ratings = {
  id: string;
  review: string;
  rating: number;
};
